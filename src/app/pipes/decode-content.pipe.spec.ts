import { DecodeContentPipe } from './decode-content.pipe';

describe('DecodeContentPipe', () => {
  it('create an instance', () => {
    const pipe = new DecodeContentPipe();
    expect(pipe).toBeTruthy();
  });
});
