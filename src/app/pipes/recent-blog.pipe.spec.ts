import { RecentBlogPipe } from './recent-blog.pipe';

describe('RecentBlogPipe', () => {
  it('create an instance', () => {
    const pipe = new RecentBlogPipe();
    expect(pipe).toBeTruthy();
  });
});
